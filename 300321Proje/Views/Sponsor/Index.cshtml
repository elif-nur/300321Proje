@model IEnumerable<Entities.Concrete.Sponsor>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/ManagerPanel/_ManagerLayout.cshtml";
}

<br />
<p>
    <a href="/Sponsor/Create" class="btn btn-success">Ekle</a>
</p>
<table class="table" id="tablo">
    <thead>
        <tr>
            <th>ID</th>
            <th>RESİM</th>
            <th>LINK</th>
            <th>SİL</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td><img src=@item.ImagePath style="width:170px; height:150px" /></td>
                <td>@item.Link</td>
                <td><a href="/Sponsor/Delete/@item.Id" class="btn btn-danger">Sil</a></td>
            </tr>
        }
    </tbody>

</table>


