
@{
    ViewBag.Title = "Submit_Abstract";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .contact .info-box {
        height: 150px
    }
</style>




<section id="contact" class="contact">

    <div class="container">
        <div id="english">
            <h5>Submit Abstract</h5>
            <div class="bBorder"></div>
            <br />
            <div class="row">
                <div class="col-lg-12">
                    <div class="info-box mb-12">
                        <form action="Submit_Abstract" method="post" enctype="multipart/form-data">
                            <input type="file" name="Filee" id="Filee" style="margin-top:45px" required />

                            <input type="submit" class="btn btn-secondary" />
                            @if (TempData["Success"] != null)
                            {
                                <div class="success">
                                    <br />
                                    <p>@TempData["Success"].ToString()</p>
                                </div>
                            }
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div id="arabic">
            <h5>إرسال الملخص</h5>
            <div class="bBorder"></div>
            <br />
            <div class="row">
                <div class="col-lg-12">
                    <div class="info-box mb-12">
                        <form action="Submit_Abstract" method="post" enctype="multipart/form-data">
                            <input type="file" name="Filee" id="Filee" style="margin-top:45px" required />

                            <input type="submit" class="btn btn-secondary" />
                            @if (TempData["Success"] != null)
                            {
                                <div class="success">
                                    <br />
                                    <p>@TempData["Success"].ToString()</p>
                                </div>
                            }
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <div id="turkish">
            <h5>Özet Gönder</h5>
            <div class="bBorder"></div>
            <br />
            <div class="row">
                <div class="col-lg-12">
                    <div class="info-box mb-12">
                        <form action="Submit_Abstract" method="post" enctype="multipart/form-data">
                            <input type="file" name="Filee" id="Filee" style="margin-top:45px" required />

                            <input type="submit" class="btn btn-secondary" value="Gönder" />
                            @if (TempData["Success"] != null)
                            {
                                <div class="success">
                                    <br />
                                    <p>@TempData["Success"].ToString()</p>
                                </div>
                            }
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

