@model IEnumerable<Entities.Concrete.Contact>
@{
    ViewBag.Title = "Contact";
}

<section id="contact" class="contact">
   
    <div class="container">
       
        <div id="turkish">
            <h5>İletişim</h5>
            <div class="bBorder"></div><br />
            <div class="row">
                <div class="col-lg-6">
                    <div class="info-box mb-4">
                        <i class="bx bx-map"></i>
                        <h3>Adresimiz</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Adress</p>
                        }

                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-envelope"></i>
                        <h3>E-posta</h3>
                        @foreach (var item in Model)
                        {
                            <p><a href="mailto:@item.Email">@item.Email</a></p>
                        }
                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-phone-call"></i>
                        <h3>Telefon</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Number</p>
                        }
                    </div>
                </div>

            </div>
        </div>
        <div id="english">
            <h5>Contact</h5>
            <div class="bBorder"></div><br />
            <div class="row">
                <div class="col-lg-6">
                    <div class="info-box mb-4">
                        <i class="bx bx-map"></i>
                        <h3>Our Address</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Adress</p>
                        }

                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-envelope"></i>
                        <h3>Email Us</h3>
                        @foreach (var item in Model)
                        {
                            <p><a href="mailto:@item.Email">@item.Email</a></p>
                        }
                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-phone-call"></i>
                        <h3>Call Us</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Number</p>
                        }
                    </div>
                </div>

            </div>
        </div>
        <div id="arabic">
            <h5>اتصال</h5>
            <div class="bBorder"></div><br />
            <div class="row">
                <div class="col-lg-6">
                    <div class="info-box mb-4">
                        <i class="bx bx-map"></i>
                        <h3>عنواننا</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Adress</p>
                        }

                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-envelope"></i>
                        <h3>البريد الإلكتروني</h3>
                        @foreach (var item in Model)
                        {
                            <p><a href="mailto:@item.Email">@item.Email</a></p>
                        }
                    </div>
                </div>

                <div class="col-lg-3">
                    <div class="info-box  mb-4">
                        <i class="bx bx-phone-call"></i>
                        <h3>هاتف</h3>
                        @foreach (var item in Model)
                        {
                            <p>@item.Number</p>
                        }
                    </div>
                </div>

            </div>
        </div>
        <div class="row">

            <div class="col-lg-12 ">

                <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3142.814424282821!2d32.50282756532502!3d38.028105579714016!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x14d08d163aae097d%3A0xb70fadb3a7e63d22!2zxLBzbGFtaSDEsGxpbWxlciBGYWvDvGx0ZXNp!5e0!3m2!1str!2str!4v1617797153936!5m2!1str!2str" frameborder="0" style="border:0; width: 100%; height: 384px;" allowfullscreen></iframe>
            </div>

            @*<div class="col-lg-6">
                <form action="forms/contact.php" method="post" role="form" class="php-email-form">
                    <div class="row">
                        <div class="col-md-6 form-group">
                            <input type="text" name="name" class="form-control" id="name" placeholder="Your Name" required>
                        </div>
                        <div class="col-md-6 form-group mt-3 mt-md-0">
                            <input type="email" class="form-control" name="email" id="email" placeholder="Your Email" required>
                        </div>
                    </div>
                    <div class="form-group mt-3">
                        <input type="text" class="form-control" name="subject" id="subject" placeholder="Subject" required>
                    </div>
                    <div class="form-group mt-3">
                        <textarea class="form-control" name="message" rows="5" placeholder="Message" required></textarea>
                    </div>
                    <div class="my-3">
                        <div class="loading">Loading</div>
                        <div class="error-message"></div>
                        <div class="sent-message">Your message has been sent. Thank you!</div>
                    </div>
                    <div class="text-center"><button type="submit">Send Message</button></div>
                </form>
            </div>*@

        </div>

    </div>
</section>

